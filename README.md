# Alpha Discovery Engine MVP

This repository contains the MVP scaffold for the alpha discovery engine.

## Run
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
